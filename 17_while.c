#include <stdio.h>

int main() {

    int i = 0;

    while (i < 10) 
    {
        printf("%d\n", i);
        i++; //kontrol ifadesi döngü içinde değiştirilmesi gerekir.
    }

    return 0;
}